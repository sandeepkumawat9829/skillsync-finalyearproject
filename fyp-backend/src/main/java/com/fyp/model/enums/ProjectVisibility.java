package com.fyp.model.enums;

/**
 * Enum for project visibility settings.
 * Replaces hardcoded "PUBLIC"/"PRIVATE" strings for type safety.
 */
public enum ProjectVisibility {
    PUBLIC,
    PRIVATE
}
